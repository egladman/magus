// @error: boom
// An error raised inside a fiber surfaces out of the driving foreach.
fun bad() > void *> int? {
    _ = yield 1;
    throw "boom";
}
var sum = 0;
foreach (n in &bad()) {
    sum = sum + n;
}
final __r = sum;
