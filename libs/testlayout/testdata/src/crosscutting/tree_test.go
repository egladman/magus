// No tree.go, but tree_linux.go and tree_darwin.go are the platform family this
// covers, so it pairs and stays silent.
package crosscutting

import "testing"

func TestTree(t *testing.T) { _ = Widget{} }
