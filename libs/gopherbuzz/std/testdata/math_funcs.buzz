// @expect: [5, 2, 1, 3, 1024, 0, 1]
import "math";
final __r = [
    math.abs(0 - 5),
    math.ceil(1.2),
    math.floor(1.8),
    math.sqrt(9.0),
    math.pow(2.0, 10.0),
    math.sin(0.0),
    math.cos(0.0),
];
