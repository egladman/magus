// @error: overlapping match condition
// Ranges are HALF-OPEN, so 1..5 is [1,5) and overlaps [4,8) at 4. Adjacent
// ranges like 0..5 and 5..10 do NOT overlap - see 63_match.buzz, which relies on it.
final __r = match (4) { 1..5 -> "left", 4..8 -> "right", else -> "other" };
