// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the env host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export extern fun expand(s: str) > str;
export extern fun get(name: str) > str;
export extern fun getOr(name: str, def: str) > str;
export extern fun home() > str !> any;
export extern fun list() > {str: str};
export extern fun loadDotenv(path: str) > void !> any;
export extern fun lookup(name: str) > [any];
export extern fun parseDotenv(content: str) > {str: str};
export extern fun readDotenv(path: str) > {str: str} !> any;
export extern fun require(name: str) > str !> any;
export extern fun set(name: str, value: str) > void !> any;
export extern fun unset(name: str) > void !> any;
