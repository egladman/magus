// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the vcs host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export object Path {
    value: str = "",
    base: str = "",
    isDir: bool = false,
}

export object ExecResult {
    stdout: str = "",
    stderr: str = "",
    code: int = 0,
    ok: bool = false,
}

export object CommitAuthor {
    name: str = "",
    email: str = "",
}

export object Commit {
    id: str = "",
    short: str = "",
    author: CommitAuthor = CommitAuthor{},
    date: str = "",
    subject: str = "",
    body: str = "",
    parents: [str] = [],
}

export object Status {
    clean: bool = false,
    files: [Path] = [],
}

export object SemverVersion {
    major: int = 0,
    minor: int = 0,
    patch: int = 0,
    prerelease: str = "",
    metadata: str = "",
    original: str = "",
}

export object Tag {
    name: str = "",
    prefix: str = "",
    version: SemverVersion = SemverVersion{},
    date: str = "",
    id: str = "",
}

export extern fun base() > str;
export extern fun changedFiles(base: str = "") > [Path] !> any;
export extern fun cmd(args: [str], opts: {str: any} = {<str: any>}) > ExecResult !> any;
export extern fun commit(rev: str = "") > Commit !> any;
export extern fun describe() > str !> any;
export extern fun dirtyDiff(paths: [str] = [<str>]) > str;
export extern fun history(limit: int = 10) > [Commit] !> any;
export extern fun isDirty(paths: [str] = [<str>]) > bool !> any;
export extern fun name() > str;
export extern fun ref() > str !> any;
export extern fun root() > str !> any;
export extern fun status(paths: [str] = [<str>]) > Status !> any;
export extern fun tags(pattern: str = "") > [Tag] !> any;
