// Code generated by magus-bindings-gen. DO NOT EDIT.

package luagen

import (
	"context"

	"github.com/egladman/magus/internal/interp/engine"
	lua "github.com/egladman/magus/internal/interp/engine/lua"
	"github.com/egladman/magus/internal/std"
)

var _ = context.Background
var _ lua.Session

// RegisterVcs builds the "vcs" module table and returns it.
// Version-control queries for the current working tree.
func RegisterVcs(r lua.Session) engine.Table {
	m := r.NewTable()
	{
		v, err := std.VcsName()
		_ = err
		m.RawSetString("name", engine.StringValue(v))
	}
	{
		v, err := std.VcsBase()
		_ = err
		m.RawSetString("base", engine.StringValue(v))
	}
	m.RawSetString("root", r.NewFunction(vcsRoot))
	m.RawSetString("diff", r.NewFunction(vcsDiff))
	m.RawSetString("short_hash", r.NewFunction(vcsShortHash))
	m.RawSetString("hash", r.NewFunction(vcsHash))
	m.RawSetString("branch", r.NewFunction(vcsBranch))
	m.RawSetString("commit_date", r.NewFunction(vcsCommitDate))
	m.RawSetString("is_dirty", r.NewFunction(vcsIsDirty))
	m.RawSetString("metadata", r.NewFunction(vcsMetadata))
	m.RawSetString("commit", r.NewFunction(vcsCommit))
	m.RawSetString("history", r.NewFunction(vcsHistory))
	m.RawSetString("exe", r.NewFunction(vcsExe))
	return m
}

// vcsRoot wraps std.VcsRoot.
// Absolute path of the repository root.
func vcsRoot(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsRoot(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// vcsDiff wraps std.VcsDiff.
// List files changed against the given base (defaults to vcs.base).
func vcsDiff(ctx context.Context, r lua.Session) int {
	base := ""
	if r.GetTop() >= 1 && !r.Get(1).IsNil() {
		base = r.CheckString(1)
	}
	ret0, err := std.VcsDiff(ctx, base)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	{
		tbl := r.NewTable()
		for i, v := range ret0 {
			tbl.RawSetInt(i+1, engine.StringValue(v))
		}
		r.Push(tbl)
	}
	return 1
}

// vcsShortHash wraps std.VcsShortHash.
// Short commit hash, or empty on error.
func vcsShortHash(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsShortHash(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// vcsHash wraps std.VcsHash.
// Full commit hash, or empty on error.
func vcsHash(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsHash(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// vcsBranch wraps std.VcsBranch.
// Current branch, or empty on error.
func vcsBranch(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsBranch(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// vcsCommitDate wraps std.VcsCommitDate.
// Commit date string, or empty on error.
func vcsCommitDate(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsCommitDate(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// vcsIsDirty wraps std.VcsIsDirty.
// True if the working tree has uncommitted changes.
func vcsIsDirty(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsIsDirty(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.BoolValue(ret0))
	return 1
}

// vcsMetadata wraps std.VcsMetadata.
// Full metadata table: short_hash, hash, branch, commit_date, is_dirty.
func vcsMetadata(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsMetadata(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	pushAnyMap(r, ret0)
	return 1
}

// vcsCommit wraps std.VcsCommit.
// Resolve a revision (a VCS-native rev expression; omit for the current revision) to its commit record: {id, short, author {name, email}, date, subject, body, parents}. id is the content/revision id (git SHA, hg node, jj commit_id); date is RFC3339, when the revision was recorded. Every field is meaningful for every VCS. Returns nil when no VCS is resolved or the revision can't be looked up.
func vcsCommit(ctx context.Context, r lua.Session) int {
	rev := ""
	if r.GetTop() >= 1 && !r.Get(1).IsNil() {
		rev = r.CheckString(1)
	}
	ret0, err := std.VcsCommit(ctx, rev)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	/* TODO: push any */ _ = ret0
	return 1
}

// vcsHistory wraps std.VcsHistory.
// Up to limit recent commits, newest first; each is the same record vcs.commit returns. An empty list when no VCS is resolved.
func vcsHistory(ctx context.Context, r lua.Session) int {
	limit := r.CheckInt(1)
	ret0, err := std.VcsHistory(ctx, limit)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	/* TODO: push any */ _ = ret0
	return 1
}

// vcsExe wraps std.VcsExe.
// Absolute path to the active VCS executable (git/hg/jj), or "" if unresolved. Lets a magusfile run a VCS-agnostic escape-hatch command: os.exec(vcs.exe(), [...]).
func vcsExe(ctx context.Context, r lua.Session) int {
	ret0, err := std.VcsExe(ctx)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}
