// Code generated by magus-bindings-gen. DO NOT EDIT.

package luagen

import (
	"context"

	"github.com/egladman/magus/internal/interp/engine"
	lua "github.com/egladman/magus/internal/interp/engine/lua"
	"github.com/egladman/magus/internal/std"
)

var _ = context.Background
var _ lua.Session

// RegisterCrypto builds the "crypto" module table and returns it.
// Content digests (SHA-256/512; SHA-1 and MD5 for legacy-checksum interop).
func RegisterCrypto(r lua.Session) engine.Table {
	m := r.NewTable()
	m.RawSetString("sha256_hex", r.NewFunction(cryptoSha256Hex))
	m.RawSetString("sha256_file", r.NewFunction(cryptoSha256File))
	m.RawSetString("sha512_hex", r.NewFunction(cryptoSha512Hex))
	m.RawSetString("sha512_file", r.NewFunction(cryptoSha512File))
	m.RawSetString("sha1_hex", r.NewFunction(cryptoSha1Hex))
	m.RawSetString("sha1_file", r.NewFunction(cryptoSha1File))
	m.RawSetString("md5_hex", r.NewFunction(cryptoMd5Hex))
	m.RawSetString("md5_file", r.NewFunction(cryptoMd5File))
	return m
}

// cryptoSha256Hex wraps std.CryptoSha256Hex.
// Return the lowercase hex SHA-256 digest of data.
func cryptoSha256Hex(ctx context.Context, r lua.Session) int {
	data := r.CheckString(1)
	ret0, err := std.CryptoSha256Hex(ctx, data)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoSha256File wraps std.CryptoSha256File.
// Return the lowercase hex SHA-256 digest of the file at path.
func cryptoSha256File(ctx context.Context, r lua.Session) int {
	path := r.CheckString(1)
	ret0, err := std.CryptoSha256File(ctx, path)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoSha512Hex wraps std.CryptoSha512Hex.
// Return the lowercase hex SHA-512 digest of data.
func cryptoSha512Hex(ctx context.Context, r lua.Session) int {
	data := r.CheckString(1)
	ret0, err := std.CryptoSha512Hex(ctx, data)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoSha512File wraps std.CryptoSha512File.
// Return the lowercase hex SHA-512 digest of the file at path.
func cryptoSha512File(ctx context.Context, r lua.Session) int {
	path := r.CheckString(1)
	ret0, err := std.CryptoSha512File(ctx, path)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoSha1Hex wraps std.CryptoSha1Hex.
// Return the lowercase hex SHA-1 digest of data. For interop with legacy/git checksums only — SHA-1 is not collision-resistant; use sha256 for anything security-relevant.
func cryptoSha1Hex(ctx context.Context, r lua.Session) int {
	data := r.CheckString(1)
	ret0, err := std.CryptoSha1Hex(ctx, data)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoSha1File wraps std.CryptoSha1File.
// Return the lowercase hex SHA-1 digest of the file at path. For interop with legacy/git checksums only — SHA-1 is not collision-resistant; use sha256 for anything security-relevant.
func cryptoSha1File(ctx context.Context, r lua.Session) int {
	path := r.CheckString(1)
	ret0, err := std.CryptoSha1File(ctx, path)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoMd5Hex wraps std.CryptoMd5Hex.
// Return the lowercase hex MD5 digest of data. For interop with legacy checksum manifests only — MD5 is broken; use sha256 for anything security-relevant.
func cryptoMd5Hex(ctx context.Context, r lua.Session) int {
	data := r.CheckString(1)
	ret0, err := std.CryptoMd5Hex(ctx, data)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// cryptoMd5File wraps std.CryptoMd5File.
// Return the lowercase hex MD5 digest of the file at path. For interop with legacy checksum manifests only — MD5 is broken; use sha256 for anything security-relevant.
func cryptoMd5File(ctx context.Context, r lua.Session) int {
	path := r.CheckString(1)
	ret0, err := std.CryptoMd5File(ctx, path)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}
