// Code generated by magus-bindings-gen. DO NOT EDIT.

package luagen

import (
	"context"

	"github.com/egladman/magus/internal/interp/engine"
	lua "github.com/egladman/magus/internal/interp/engine/lua"
	"github.com/egladman/magus/internal/std"
)

var _ = context.Background
var _ lua.Session

// RegisterPlatform builds the "platform" module table and returns it.
// Normalize OS/architecture identifiers across naming conventions (aarch64↔arm64, Darwin↔darwin).
func RegisterPlatform(r lua.Session) engine.Table {
	m := r.NewTable()
	m.RawSetString("arch", r.NewFunction(platformArch))
	m.RawSetString("os", r.NewFunction(platformOs))
	return m
}

// platformArch wraps std.PlatformArch.
// Normalize an architecture identifier (x86_64, aarch64, armv7l, …) to canonical Go GOARCH (amd64, arm64, arm). With style, render that result in a convention (go|uname); raises on an unknown style. Returns "" when the identifier is unrecognized.
func platformArch(ctx context.Context, r lua.Session) int {
	name := r.CheckString(1)
	style := ""
	if r.GetTop() >= 2 && !r.Get(2).IsNil() {
		style = r.CheckString(2)
	}
	ret0, err := std.PlatformArch(ctx, name, style)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}

// platformOs wraps std.PlatformOS.
// Normalize an OS identifier (Darwin, macOS, win, …) to canonical Go GOOS (darwin, windows). With style, render that result in a convention (go|uname); raises on an unknown style. Returns "" when the identifier is unrecognized.
func platformOs(ctx context.Context, r lua.Session) int {
	name := r.CheckString(1)
	style := ""
	if r.GetTop() >= 2 && !r.Get(2).IsNil() {
		style = r.CheckString(2)
	}
	ret0, err := std.PlatformOS(ctx, name, style)
	if err != nil {
		r.RaiseError("%s", err.Error())
		return 0
	}
	r.Push(engine.StringValue(ret0))
	return 1
}
