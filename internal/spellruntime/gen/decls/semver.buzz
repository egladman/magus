// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the semver host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export object SemverNext {
    major: str = "",
    minor: str = "",
    patch: str = "",
}

export object SemverVersion {
    major: int = 0,
    minor: int = 0,
    patch: int = 0,
    prerelease: str = "",
    metadata: str = "",
    original: str = "",
}

export extern fun canonical(v: str) > str;
export extern fun compare(a: str, b: str) > int;
export extern fun isValid(v: str) > bool;
export extern fun major(v: str) > str;
export extern fun majorMinor(v: str) > str;
export extern fun next(v: str) > SemverNext;
export extern fun parse(v: str) > SemverVersion;
export extern fun satisfies(v: str, constraint: str) > bool;
