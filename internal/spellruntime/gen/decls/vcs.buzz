// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the vcs host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export object CommitAuthor {
    name: str = "",
    email: str = "",
}

export object Commit {
    id: str = "",
    short: str = "",
    author: CommitAuthor = CommitAuthor{},
    date: str = "",
    subject: str = "",
    body: str = "",
    parents: [str] = [],
}

export object SemverVersion {
    major: int = 0,
    minor: int = 0,
    patch: int = 0,
    prerelease: str = "",
    metadata: str = "",
    original: str = "",
}

export object Tag {
    name: str = "",
    prefix: str = "",
    version: SemverVersion = SemverVersion{},
    date: str = "",
    id: str = "",
}

export extern fun branch() > str;
export extern fun commit(rev: str = "") > Commit;
export extern fun commitDate() > str;
export extern fun describe() > str;
export extern fun diagnoseDrift(outputs: [str], inputs: [str] = [<str>]) > {str: any};
export extern fun diff(base: str = "") > [str];
export extern fun dirtyFiles(paths: [str] = [<str>]) > [str];
export extern fun exe() > str;
export extern fun hash() > str;
export extern fun history(limit: int = 10) > [Commit];
export extern fun isDirty(paths: [str] = [<str>]) > bool;
export extern fun metadata() > {str: any};
export extern fun root() > str;
export extern fun shortHash() > str;
export extern fun tags(pattern: str = "") > [Tag];
