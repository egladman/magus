// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the path host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export extern fun abs(path: str) > str;
export extern fun clean(path: str) > str;
export extern fun expandUser(path: str) > str;
export extern fun isAbs(path: str) > bool;
export extern fun matches(pattern: str, path: str) > bool;
export extern fun matchesAny(patterns: [str], path: str) > bool;
export extern fun rel(base: str, target: str) > str;
