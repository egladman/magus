// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the crypto host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export enum<str> SignAlgorithm {
    Ed25519 = "ed25519",
}

export extern fun base64DecodeBytes(s: str) > [int];
export extern fun base64EncodeBytes(data: [int]) > str;
export extern fun hmacSha256(key: [int], data: [int]) > [int];
export extern fun hmacSha256Hex(key: [int], data: [int]) > str;
export extern fun md5File(path: str) > str;
export extern fun md5Hex(data: str) > str;
export extern fun publicKey(alg: SignAlgorithm, key_env: str) > str;
export extern fun sha1File(path: str) > str;
export extern fun sha1Hex(data: str) > str;
export extern fun sha256File(path: str) > str;
export extern fun sha256Hex(data: str) > str;
export extern fun sha512File(path: str) > str;
export extern fun sha512Hex(data: str) > str;
export extern fun sign(alg: SignAlgorithm, data: str, key_env: str) > str;
export extern fun signFile(alg: SignAlgorithm, path: str, key_env: str) > str;
export extern fun verify(alg: SignAlgorithm, data: str, sig_hex: str, pub_hex: str) > bool;
