// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the term host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export enum<str> TermStyle {
    none = "",
    bold = "1",
    dim = "2",
    red = "31",
    green = "32",
    yellow = "33",
    dimGreen = "2;32",
    dimGrey = "2;37",
    brightGreen = "1;32",
}

export object TermSize {
    width: int = 0,
    height: int = 0,
}

export extern fun clearScreen() > void;
export extern fun colorize(s: str, style: TermStyle) > str;
export extern fun isInteractive() > bool;
export extern fun pick(items: [str], prompt: str = "", initial_filter: str = "", initial: int = 0, max_rows: int = 0) > int;
export extern fun size() > TermSize;
export extern fun wantsColor() > bool;
