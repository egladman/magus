// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the time host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export extern fun add(unix_millis: double, duration: str) > double;
export extern fun diff(a: double, b: double) > double;
export extern fun format(layout: str, unix_millis: double) > str;
export extern fun nowIso() > str;
export extern fun parse(layout: str, value: str) > double;
export extern fun parseDuration(duration: str) > double;
