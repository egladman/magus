// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the fs host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export object Path {
    value: str = "",
    base: str = "",
    isDir: bool = false,
}

export object FileInfo {
    size: int = 0,
    mtime: double = 0.0,
    mode: int = 0,
    is_dir: bool = false,
}

export extern fun appendFile(path: str, content: str) > void;
export extern fun basename(path: str) > str;
export extern fun chmod(path: str, mode: int) > void;
export extern fun copyDir(src: str, dst: str) > void;
export extern fun copyFile(src: str, dst: str) > void;
export extern fun dirname(path: str) > str;
export extern fun exists(path: str) > bool;
export extern fun ext(path: str) > str;
export extern fun glob(pattern: str) > [Path];
export extern fun isDir(path: str) > bool;
export extern fun isFile(path: str) > bool;
// join is variadic; Buzz has no variadic parameter, so it stays untyped.
export extern fun listDir(path: str) > [str];
export extern fun mkdirAll(path: str, perm: int = 493) > void;
export extern fun readFile(path: str) > str;
export extern fun readLines(path: str) > [str];
export extern fun readlink(path: str) > str;
export extern fun removeAll(path: str) > void;
export extern fun stat(path: str) > FileInfo;
export extern fun symlink(target: str, link: str) > void;
export extern fun tempDir(prefix: str = "") > str;
export extern fun walk(root: str, callback: Function) > void;
export extern fun watch(paths: [str], callback: Function) > void;
export extern fun writeFile(path: str, content: str) > void;
export extern fun writeLines(path: str, lines: [str]) > void;
