// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the encoding host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export object URL {
    scheme: str = "",
    host: str = "",
    port: str = "",
    path: str = "",
    query: str = "",
    fragment: str = "",
}

export extern fun base64Decode(s: str) > str;
export extern fun base64Encode(data: str) > str;
export extern fun base64urlDecode(s: str) > str;
export extern fun base64urlEncode(data: str) > str;
export extern fun buildUrl(parts: {str: any}) > str;
export extern fun hexDecode(s: str) > str;
export extern fun hexEncode(data: str) > str;
export extern fun parseUrl(raw_url: str) > URL;
export extern fun urlDecode(s: str) > str;
export extern fun urlEncode(s: str) > str;
