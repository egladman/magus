// Code generated by magus-utils moduledecls. DO NOT EDIT.
//
// Buzz declarations for the archive host module: the object mirrors its methods
// return, then one `export extern fun` per method. Registered with SetModuleDecls
// beside the native implementation, so the checker reads the signature while the
// runtime keeps the host binding - an extern emits no code, so it cannot shadow
// one. Edit the std.Module declaration and rerun `go generate`, never this file.

export object Path {
    value: str = "",
    base: str = "",
    isDir: bool = false,
}

export object CompressResult {
    files: [Path] = [],
    bytes_in: int = 0,
    bytes_out: int = 0,
}

export object UncompressResult {
    files: [Path] = [],
    bytes: int = 0,
}

export extern fun compress(src: str, dest: str, opts: {str: any} = {<str: any>}) > CompressResult;
export extern fun uncompress(src: str, dest: str, opts: {str: any} = {<str: any>}) > UncompressResult;
