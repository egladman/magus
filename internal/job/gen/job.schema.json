{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://magus.invalid/job/job.schema.json",
  "$comment": "Generated from Declaration by `magus-utils jobschema`. DO NOT EDIT; run `magus run job-generate .`.",
  "title": "magus job",
  "description": "Declaration is the typed INPUT for one lease row: the fields a caller DECLARES, and nothing the store computes. A caller cannot say when its row was created or what it released, and the way to make that true is for the input type not to carry those fields rather than for the store to strip them afterwards.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "id"],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 7,
      "description": "SchemaVersion is the row shape this record is written in, and it is required: a version this magus does not know is rejected by name. See JobSchemaVersion."
    },
    "id": {
      "type": "string",
      "pattern": "^[./0-9:A-Z_a-z-]+$",
      "maxLength": 128,
      "description": "ID is the lease's identity within the plan, the key a second register replaces on, and the only required field besides the version."
    },
    "parent": {
      "type": "string",
      "description": "Parent is the lease this one was spawned under, empty for a lease the root declared."
    },
    "criteria": {
      "type": "string",
      "description": "Criteria is what this lease is for and what done means, as one block of prose. The machine-checkable half is CompletionGates."
    },
    "checkpoint": {
      "type": "string",
      "description": "Checkpoint is the working state this lease starts from, as `magus vcs checkpoint -o name` prints it."
    },
    "write_paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "WritePaths is the declared write lane, empty on a read-only row by design."
    },
    "deny_paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "DenyPaths are the paths inside that lane this lease may not write."
    },
    "read_paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "ReadPaths is the declared READ lane, widened to those projects' dependencies by the guard. Empty means write_paths stands in."
    },
    "depends_on": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "DependsOn names the lease ids that must land before this one."
    },
    "model": {
      "type": "string",
      "description": "Model is the model or effort tier the work was matched to, a free string because hosts name their models differently."
    },
    "owned_paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "LegacyWritePaths is the pre-rename spelling of write_paths, accepted on input and never emitted."
    },
    "forbidden_paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "LegacyDenyPaths is the pre-rename spelling of deny_paths. compat: see LegacyWritePaths."
    },
    "focus": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "LegacyReadPaths is the pre-rename spelling of read_paths. compat: see LegacyWritePaths."
    },
    "tier": {
      "type": "string",
      "description": "LegacyModel is the pre-rename spelling of model. compat: see LegacyWritePaths."
    },
    "check": {
      "type": "object",
      "additionalProperties": false,
      "required": ["target"],
      "properties": {
        "target": {
          "type": "string",
          "description": "Target is the magus target the check runs, without the `magus run` in front of it."
        },
        "project": {
          "type": "string",
          "description": "Project is the project the target runs in, \".\" when the row names none."
        },
        "args": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Args are forwarded to the tool after `--`, never to magus."
        }
      },
      "description": "Check is the one check this lease runs, and acceptance binds a worker's evidence to it."
    },
    "state": {
      "type": "string",
      "enum": ["", "declared", "running", "exited", "pass", "fail", "no_return"],
      "description": "State is the row's lifecycle position, empty for a row that has not said where it stands. no_return is a lease that never reported, which is not a failure."
    },
    "read_only": {
      "type": "boolean",
      "description": "ReadOnly marks a lease that gathers evidence and writes nothing, so empty write paths are correct rather than missing."
    },
    "validation": {
      "type": "string",
      "description": "Validation is the check as a rendered `magus run` line."
    },
    "completion_gates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "kind", "expect"],
        "properties": {
          "id": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "depends_on": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "kind": {
            "type": "string",
            "enum": ["check", "paths", "symbol"],
            "description": "Kind names WHAT this gate examines and Expect names what must be true of it. Two fields rather than one, because the alternative is a kind per pair: `paths` beside `exists` beside `absent` beside `no-symbol`, which is a vocabulary that grows by multiplication and reads inconsistently the moment it has four members."
          },
          "expect": {
            "type": "string",
            "enum": ["passed", "changed", "present", "absent", "unreferenced"]
          },
          "check": {
            "type": "object",
            "additionalProperties": false,
            "required": ["target"],
            "properties": {
              "target": {
                "type": "string",
                "description": "Target is the magus target the check runs, without the `magus run` in front of it."
              },
              "project": {
                "type": "string",
                "description": "Project is the project the target runs in, \".\" when the row names none."
              },
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Args are forwarded to the tool after `--`, never to magus."
              }
            },
            "description": "Check is the run a GateKindCheck gate examines. Zero on every other kind."
          },
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Paths are the globs a GateKindPaths gate examines. Zero on every other kind."
          },
          "symbols": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Symbols are the names a GateKindSymbol gate examines, as the knowledge graph resolves them. Zero on every other kind."
          }
        }
      },
      "description": "CompletionGates declare additional evidence-backed acceptance conditions."
    },
    "timeout": {
      "type": "string",
      "description": "Timeout bounds the lease, as a Go duration. The store stamps Job.Deadline from it when it writes the row, so a declaration carries a length and never an instant. Empty is no bound; there is no default here (a workspace may set jobs.default_timeout)."
    }
  }
}
