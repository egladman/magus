.TH MAGUS-SERVER 1 "" "magus" "magus Manual"
.SH NAME
magus\-server \- Manage the magus server: MCP, the console, APIs and background jobs
.SH SYNOPSIS
.B magus
.RI "server <start|stop|status|reload> [flags]"
.SH DESCRIPTION
Start, stop, reload or check the magus server.
.PP
The server is the background process a person asks for. It serves MCP and the
console over HTTP, the APIs behind them, background jobs and scheduled
maintenance, and keeps each workspace's knowledge graph and symbol indexes
current. It keeps workspaces warm, so nested magus calls that forward to it pay
for discovery and config once. Nothing starts it but `magus server start` (and
`graph export \-\-follow`, which asks for the console by name), and it runs
until stopped.
.PP
It is not what holds this host's capacity: that is `magus broker`, which a
run starts on its own. The server asks the broker like any run does.
.PP
The socket address is resolved in priority order:
  \-\-socket flag  >  MAGUS_SERVER_ADDRESS env  >  server.address in magus.yaml  >
  default ($XDG_RUNTIME_DIR/magus/server.sock)
.PP
A detached server logs to $XDG_STATE_HOME/magus/server.log; under
\-\-foreground it logs to stderr for the supervisor to keep.
.PP
Signals:
  SIGHUP           reload configuration, the same as `magus server reload`
  SIGTERM, SIGINT  stop: close the socket, cancel the runs it adopted, and
                   wait up to shutdown_grace (default 5m) for them to unwind
  a second one     exit now
.SS server start options
.RS
.TP
\fB\-\-foreground\fR
Run in the foreground and block, instead of auto\-backgrounding
.RE
.SS server stop options
.RS
.TP
\fB\-\-socket\fR \fIstring\fR
Server socket (default: config / MAGUS_SERVER_ADDRESS / server.sock)
.RE
.SS server status options
.RS
.TP
\fB\-\-socket\fR \fIstring\fR
Server socket (default: config / MAGUS_SERVER_ADDRESS / server.sock)
.RE
.SS server reload options
.RS
.TP
\fB\-\-socket\fR \fIstring\fR
Server socket (default: config / MAGUS_SERVER_ADDRESS / server.sock)
.RE
.SH SUBCOMMANDS
.RS
.TP
\fBstart\fR
Start the server (auto\-backgrounds by default; \-\-foreground blocks)
.TP
\fBstop\fR
Send a graceful shutdown request to the running server
.TP
\fBstatus\fR
The server: whether it is up and where you reach it
.TP
\fBreload\fR
Re\-read configuration without restarting: drop the server's open workspaces
.RE
.SH EXAMPLES
\fIStart the server (auto\-backgrounds)\fR
.PP
.EX
magus server start
.EE
.PP
\fIRun the server in the foreground (supervisor or debugging)\fR
.PP
.EX
magus server start --foreground
.EE
.PP
\fIStop the running server\fR
.PP
.EX
magus server stop
.EE
.PP
\fIReload configuration without restarting\fR
.PP
.EX
magus server reload
.EE
.PP
\fIEverything running on this host\fR
.PP
.EX
magus status
.EE
.PP
\fIUse a custom socket path\fR
.PP
.EX
magus --server-address unix:///tmp/m.sock server start
.EE
.PP
.SH SEE ALSO
\fBmagus\fR(1),
\fBmagus\-ls\fR(1),
\fBmagus\-describe\fR(1),
\fBmagus\-run\fR(1),
\fBmagus\-x\fR(1),
\fBmagus\-where\fR(1),
\fBmagus\-affected\fR(1),
\fBmagus\-graph\fR(1),
\fBmagus\-query\fR(1),
\fBmagus\-explain\fR(1),
\fBmagus\-path\fR(1),
\fBmagus\-refs\fR(1),
\fBmagus\-watch\fR(1),
\fBmagus\-events\fR(1),
\fBmagus\-status\fR(1),
\fBmagus\-clean\fR(1),
\fBmagus\-shell\fR(1),
\fBmagus\-vcs\fR(1),
\fBmagus\-queue\fR(1),
\fBmagus\-doctor\fR(1),
\fBmagus\-config\fR(1),
\fBmagus\-session\fR(1),
\fBmagus\-memory\fR(1),
\fBmagus\-job\fR(1),
\fBmagus\-notes\fR(1),
\fBmagus\-diff\fR(1),
\fBmagus\-broker\fR(1),
\fBmagus\-mcp\fR(1),
\fBmagus\-buzz\fR(1),
\fBmagus\-completion\fR(1),
\fBmagus\-man\fR(1),
\fBmagus\-init\fR(1),
\fBmagus\-spell\fR(1),
\fBmagus\-agent\fR(1),
\fBmagus\-self\fR(1),
\fBmagus\-version\fR(1)
.br
