.TH MAGUS-SESSION 1 "" "magus" "magus Manual"
.SH NAME
magus\-session \- What magus invocations did and what agents are blocked on: humans read and dispose, hosts write
.SH SYNOPSIS
.B magus
.RI "session [ls] [flags]"
.SH DESCRIPTION
One noun over the repository's session store, with a human side and a
machine side.
.PP
Humans read it. The bare command lists past magus invocations with the targets
each one ran and how those runs ended, the OS user that ran it, and the host
session it ran in (a dash when no host delivered one: the invocation is
unattributed); `session attention` lists the requests
agents have raised \- work blocked on input or on approval \- and
`session dispose` closes one. Nothing closes a request automatically: there is
no expiry, no severity inference and no auto\-dispose flag, because a request
magus could answer by itself would not have needed a person \- see the doctrine
page.
.PP
Agent hosts write it. Their hooks pipe every judged command through `magus shell`,
which records what it judged, and every attention event through `session notify`
(normalized; a waiting or permission outcome opens a durable request). No person
types notify; it exists to be wired. `magus shell` is the exception, and
deliberately so: it is the same command a person runs to ask what this workspace
would rather they ran.
.PP
The store is keyed by repository identity rather than by checkout path, so
every git worktree of one repo reads and writes the same records \- what
another worktree just finished, and what it is blocked on, is visible here
without a daemon, a network, or a shared branch. It is append\-only and never
rewritten; a line left half\-written by a killed process is skipped and counted
rather than failing the read.
.PP
The listing takes \-\-limit to bound by count and \-\-since to bound by AGE, as a
duration back from now (2h, 45m, 168h) or an RFC3339 instant. \-\-since compares
against each invocation's last fact, not its first, so a long one that is still
working stays listed however long ago it began.
.PP
\-\-brief answers the listing's own question, where does the work stand, for a
model instead of a person. It prints this checkout read off disk: branch and
revision, commits not yet on the base ref, the dirty tree split by the
classifier `magus describe file` uses, the live leases with the command that
binds each one, the last recorded run's failures with the ref that holds their
output, whether any host hook config here invokes magus, and where this
workspace's rules live. Nothing in it is remembered: an agent host that
replaces a session's history with a summary can wire this to its
session\-start event and hand the model state instead of prose.
.SH OPTIONS
.RS
.TP
\fB\-\-brief\fR
Print this checkout's state for a session that lost its history: revision, unpushed commits, classified dirty tree, live leases, the last run's failures, guard wiring (\-\-limit and \-\-since do not apply)
.TP
\fB\-\-limit\fR \fIint\fR
Show at most this many invocations (0 for all)
.TP
\fB\-\-since\fR \fIstring\fR
Show only invocations active since this point: a duration back from now (2h, 45m, 168h) or an RFC3339 timestamp
.RE
.SS session load options
.RS
.TP
\fB\-\-file\fR \fIstring\fR
Read the event stream from this file instead of stdin
.RE
.SS session dispose options
.RS
.TP
\fB\-\-reason\fR \fIstring\fR
Record why the request is being closed, alongside the disposition
.RE
.SS session checkpoint options
.RS
.TP
\fB\-\-agent\-name\fR \fIstring\fR
Name of the agent host this session ran on, when one did (attribution only)
.TP
\fB\-\-note\fR \fIstring\fR
A sentence on where the work stands
.TP
\fB\-\-session\fR \fIstring\fR
The host's own session id for this session
.TP
\fB\-\-transcript\fR \fIstring\fR
Path to the host's own log of this session, recorded as a pointer; magus never opens it
.RE
.SS session notify options
.RS
.TP
\fB\-\-desktop\fR
Also raise an OS notification
.TP
\fB\-\-outcome\fR \fIstring\fR
Outcome vocabulary for the event
.RE
.SH SUBCOMMANDS
.RS
.TP
\fBls\fR
List past magus invocations and the targets they ran (the default)
.TP
\fBload\fR
Load a normalized agent\-session event stream from a host transcript
.TP
\fBshow\fR
Report one loaded session: what it ran, what the rules say, what it loaded
.TP
\fBhints\fR
Report uptake per hint id: served, followed, rejected, reflex
.TP
\fBattention\fR
List the open requests agents raised, oldest first; with \-q, print nothing and exit 1 when the queue is empty
.TP
\fBdispose\fR
Close one open request by id or unambiguous id prefix
.TP
\fBcheckpoint\fR
Record where the work stands, so it can be picked up later
.TP
\fBnotify\fR
Normalize an attention event and optionally notify the local desktop
.RE
.SH EXIT STATUS
.RS
.TP
\fB0\fR
Sessions or requests were listed, a request was disposed, or an event was normalized and emitted. A plain listing exits 0 whether or not anything was listed, because an empty queue is the good state. notify's delivery is best\-effort and never changes this: a desktop notification that could not be raised, and a durable request that could not be opened, are both reported as warnings and still exit 0.
.TP
\fB1\fR
dispose: the request named is not in the store, or was already disposed; a request closes once and stays closed. attention with \-q: the queue is empty, so a prompt or watch loop can branch on status instead of parsing the listing. notify: stdin could not be read. Text that is not a complete event envelope becomes the event's message rather than an error. load: at least one line was rejected; usable lines are still loaded and the summary is printed, so fix the adapter and run it again. show: the session named has no loaded events.
.TP
\fB2\fR
Misuse: an unknown subcommand, an argument to a listing, or a dispose naming other than exactly one id.
.RE
.SH EXAMPLES
\fIShow recent invocations\fR
.PP
.EX
magus session
.EE
.PP
\fIShow today's work\fR
.PP
.EX
magus session --since 24h
.EE
.PP
\fIHand a compacted session this checkout's state\fR
.PP
.EX
magus session --brief
.EE
.PP
\fIFull session records as JSON\fR
.PP
.EX
magus session -o json
.EE
.PP
\fILoad a host transcript an adapter normalized\fR
.PP
.EX
magus session load --file events.ndjson
.EE
.PP
\fIRead one loaded session back\fR
.PP
.EX
magus session show 8f1c2d4e
.EE
.PP
\fIList open attention requests\fR
.PP
.EX
magus session attention
.EE
.PP
\fIAsk whether anyone is waiting\fR
.PP
.EX
magus session attention -q
.EE
.PP
\fIClose one request, saying why\fR
.PP
.EX
magus session dispose att-3f9c -reason "approved and pushed by hand"
.EE
.PP
\fIRaise a permission prompt on the desktop (host\-wired)\fR
.PP
.EX
printf '%s\(rsn' 'needs approval' | magus session notify --outcome permission --desktop
.EE
.PP
.SH SEE ALSO
\fBmagus\fR(1),
\fBmagus\-ls\fR(1),
\fBmagus\-describe\fR(1),
\fBmagus\-run\fR(1),
\fBmagus\-x\fR(1),
\fBmagus\-where\fR(1),
\fBmagus\-affected\fR(1),
\fBmagus\-graph\fR(1),
\fBmagus\-query\fR(1),
\fBmagus\-explain\fR(1),
\fBmagus\-path\fR(1),
\fBmagus\-refs\fR(1),
\fBmagus\-watch\fR(1),
\fBmagus\-events\fR(1),
\fBmagus\-status\fR(1),
\fBmagus\-clean\fR(1),
\fBmagus\-shell\fR(1),
\fBmagus\-vcs\fR(1),
\fBmagus\-queue\fR(1),
\fBmagus\-doctor\fR(1),
\fBmagus\-config\fR(1),
\fBmagus\-memory\fR(1),
\fBmagus\-job\fR(1),
\fBmagus\-notes\fR(1),
\fBmagus\-diff\fR(1),
\fBmagus\-server\fR(1),
\fBmagus\-mcp\fR(1),
\fBmagus\-buzz\fR(1),
\fBmagus\-completion\fR(1),
\fBmagus\-man\fR(1),
\fBmagus\-init\fR(1),
\fBmagus\-spell\fR(1),
\fBmagus\-agent\fR(1),
\fBmagus\-self\fR(1),
\fBmagus\-version\fR(1)
.br
