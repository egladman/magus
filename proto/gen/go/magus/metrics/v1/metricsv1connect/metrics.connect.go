// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: magus/metrics/v1/metrics.proto

// Package magus.metrics.v1 is the versioned wire contract for the DERIVED dashboard
// metrics: magus's OTel instrument families rolled up into the numbers a developer reads
// to judge health - operation counts, cache hit-rates, and latency percentiles - plus a
// rolling time-series the daemon backfills so the utilization grid shows history from
// before the page opened. A sibling of magus.status.v1 (which carries the live pool/health
// snapshot); the dashboard streams both. No service: it rides the dashboard SSE bridge as
// `event: metrics` (Snapshot) and `event: backfill` (the Sample ring buffer).
package metricsv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/egladman/magus/proto/gen/go/magus/metrics/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MetricsServiceName is the fully-qualified name of the MetricsService service.
	MetricsServiceName = "magus.metrics.v1.MetricsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MetricsServiceGetMetricsProcedure is the fully-qualified name of the MetricsService's GetMetrics
	// RPC.
	MetricsServiceGetMetricsProcedure = "/magus.metrics.v1.MetricsService/GetMetrics"
	// MetricsServiceStreamMetricsProcedure is the fully-qualified name of the MetricsService's
	// StreamMetrics RPC.
	MetricsServiceStreamMetricsProcedure = "/magus.metrics.v1.MetricsService/StreamMetrics"
)

// MetricsServiceClient is a client for the magus.metrics.v1.MetricsService service.
type MetricsServiceClient interface {
	// GetMetrics returns the current derived snapshot.
	GetMetrics(context.Context, *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error)
	// StreamMetrics pushes the rolling history first (one Backfill), then a fresh Snapshot on
	// each tick, so the dashboard's charts and utilization grid start populated and stay live.
	StreamMetrics(context.Context, *connect.Request[v1.StreamMetricsRequest]) (*connect.ServerStreamForClient[v1.StreamMetricsResponse], error)
}

// NewMetricsServiceClient constructs a client for the magus.metrics.v1.MetricsService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMetricsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MetricsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	metricsServiceMethods := v1.File_magus_metrics_v1_metrics_proto.Services().ByName("MetricsService").Methods()
	return &metricsServiceClient{
		getMetrics: connect.NewClient[v1.GetMetricsRequest, v1.GetMetricsResponse](
			httpClient,
			baseURL+MetricsServiceGetMetricsProcedure,
			connect.WithSchema(metricsServiceMethods.ByName("GetMetrics")),
			connect.WithClientOptions(opts...),
		),
		streamMetrics: connect.NewClient[v1.StreamMetricsRequest, v1.StreamMetricsResponse](
			httpClient,
			baseURL+MetricsServiceStreamMetricsProcedure,
			connect.WithSchema(metricsServiceMethods.ByName("StreamMetrics")),
			connect.WithClientOptions(opts...),
		),
	}
}

// metricsServiceClient implements MetricsServiceClient.
type metricsServiceClient struct {
	getMetrics    *connect.Client[v1.GetMetricsRequest, v1.GetMetricsResponse]
	streamMetrics *connect.Client[v1.StreamMetricsRequest, v1.StreamMetricsResponse]
}

// GetMetrics calls magus.metrics.v1.MetricsService.GetMetrics.
func (c *metricsServiceClient) GetMetrics(ctx context.Context, req *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error) {
	return c.getMetrics.CallUnary(ctx, req)
}

// StreamMetrics calls magus.metrics.v1.MetricsService.StreamMetrics.
func (c *metricsServiceClient) StreamMetrics(ctx context.Context, req *connect.Request[v1.StreamMetricsRequest]) (*connect.ServerStreamForClient[v1.StreamMetricsResponse], error) {
	return c.streamMetrics.CallServerStream(ctx, req)
}

// MetricsServiceHandler is an implementation of the magus.metrics.v1.MetricsService service.
type MetricsServiceHandler interface {
	// GetMetrics returns the current derived snapshot.
	GetMetrics(context.Context, *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error)
	// StreamMetrics pushes the rolling history first (one Backfill), then a fresh Snapshot on
	// each tick, so the dashboard's charts and utilization grid start populated and stay live.
	StreamMetrics(context.Context, *connect.Request[v1.StreamMetricsRequest], *connect.ServerStream[v1.StreamMetricsResponse]) error
}

// NewMetricsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMetricsServiceHandler(svc MetricsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	metricsServiceMethods := v1.File_magus_metrics_v1_metrics_proto.Services().ByName("MetricsService").Methods()
	metricsServiceGetMetricsHandler := connect.NewUnaryHandler(
		MetricsServiceGetMetricsProcedure,
		svc.GetMetrics,
		connect.WithSchema(metricsServiceMethods.ByName("GetMetrics")),
		connect.WithHandlerOptions(opts...),
	)
	metricsServiceStreamMetricsHandler := connect.NewServerStreamHandler(
		MetricsServiceStreamMetricsProcedure,
		svc.StreamMetrics,
		connect.WithSchema(metricsServiceMethods.ByName("StreamMetrics")),
		connect.WithHandlerOptions(opts...),
	)
	return "/magus.metrics.v1.MetricsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MetricsServiceGetMetricsProcedure:
			metricsServiceGetMetricsHandler.ServeHTTP(w, r)
		case MetricsServiceStreamMetricsProcedure:
			metricsServiceStreamMetricsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMetricsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMetricsServiceHandler struct{}

func (UnimplementedMetricsServiceHandler) GetMetrics(context.Context, *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.metrics.v1.MetricsService.GetMetrics is not implemented"))
}

func (UnimplementedMetricsServiceHandler) StreamMetrics(context.Context, *connect.Request[v1.StreamMetricsRequest], *connect.ServerStream[v1.StreamMetricsResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("magus.metrics.v1.MetricsService.StreamMetrics is not implemented"))
}
