// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: magus/notes/v1alpha1/notes.proto

// Package magus.notes.v1alpha1 is the console-facing NotesService: a READ-ONLY view over the
// workspace's human-authored notes, both the shared store in the checkout and the private
// one on this machine.
//
// It is the deliberate opposite of magus.memory.v1alpha1, and the asymmetry is the point. Memory
// is agent-written, so a human read/edit/delete surface is its safety valve. A note is
// human-written, and its whole value is that guarantee: a note is the one node class in the
// knowledge graph that is NOT derived from the workspace, so nothing in the repository
// corroborates it later and its only provenance is the person who wrote it and signed the
// commit. A browser-reachable write would put an unattributable author on that store, and
// undoing the write does not restore the guarantee. So this service has no Update and no
// Delete, and adding one is a change to the feature's central claim rather than a new RPC.
//
// No Delete even though deletion is recoverable for the shared store. The counter-intuitive
// part is worth stating so nobody "fixes" it later: SHARED notes are the safe ones to delete
// (git brings them back) and PRIVATE ones are the dangerous ones (nothing does). So if either
// store ever grew a delete it would be the shared one - which is precisely the store where
// the CLI and a reviewed commit already do the job better. Neither gets one.
//
// Access policy: every RPC requires the daemon bearer token, mounted on loopback behind the
// same guard as the other cli-token console services, and NEVER on the LAN share listener.
// The private store is by definition not shared with anyone, and a shared phone view must not
// be able to read it.
package notesv1alpha1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1alpha1 "github.com/egladman/magus/proto/gen/go/magus/notes/v1alpha1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// NotesServiceName is the fully-qualified name of the NotesService service.
	NotesServiceName = "magus.notes.v1alpha1.NotesService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// NotesServiceListNotesProcedure is the fully-qualified name of the NotesService's ListNotes RPC.
	NotesServiceListNotesProcedure = "/magus.notes.v1alpha1.NotesService/ListNotes"
	// NotesServiceGetNoteProcedure is the fully-qualified name of the NotesService's GetNote RPC.
	NotesServiceGetNoteProcedure = "/magus.notes.v1alpha1.NotesService/GetNote"
)

// NotesServiceClient is a client for the magus.notes.v1alpha1.NotesService service.
type NotesServiceClient interface {
	// ListNotes returns every note in both declared stores, each with its anchors already
	// resolved. Paginated by contract so growth never forces a breaking change, though the
	// store returns all notes today (bounded by the store's own scan cap).
	ListNotes(context.Context, *connect.Request[v1alpha1.ListNotesRequest]) (*connect.Response[v1alpha1.ListNotesResponse], error)
	// GetNote returns one note by scope and name, including its body.
	GetNote(context.Context, *connect.Request[v1alpha1.GetNoteRequest]) (*connect.Response[v1alpha1.Note], error)
}

// NewNotesServiceClient constructs a client for the magus.notes.v1alpha1.NotesService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewNotesServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) NotesServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	notesServiceMethods := v1alpha1.File_magus_notes_v1alpha1_notes_proto.Services().ByName("NotesService").Methods()
	return &notesServiceClient{
		listNotes: connect.NewClient[v1alpha1.ListNotesRequest, v1alpha1.ListNotesResponse](
			httpClient,
			baseURL+NotesServiceListNotesProcedure,
			connect.WithSchema(notesServiceMethods.ByName("ListNotes")),
			connect.WithClientOptions(opts...),
		),
		getNote: connect.NewClient[v1alpha1.GetNoteRequest, v1alpha1.Note](
			httpClient,
			baseURL+NotesServiceGetNoteProcedure,
			connect.WithSchema(notesServiceMethods.ByName("GetNote")),
			connect.WithClientOptions(opts...),
		),
	}
}

// notesServiceClient implements NotesServiceClient.
type notesServiceClient struct {
	listNotes *connect.Client[v1alpha1.ListNotesRequest, v1alpha1.ListNotesResponse]
	getNote   *connect.Client[v1alpha1.GetNoteRequest, v1alpha1.Note]
}

// ListNotes calls magus.notes.v1alpha1.NotesService.ListNotes.
func (c *notesServiceClient) ListNotes(ctx context.Context, req *connect.Request[v1alpha1.ListNotesRequest]) (*connect.Response[v1alpha1.ListNotesResponse], error) {
	return c.listNotes.CallUnary(ctx, req)
}

// GetNote calls magus.notes.v1alpha1.NotesService.GetNote.
func (c *notesServiceClient) GetNote(ctx context.Context, req *connect.Request[v1alpha1.GetNoteRequest]) (*connect.Response[v1alpha1.Note], error) {
	return c.getNote.CallUnary(ctx, req)
}

// NotesServiceHandler is an implementation of the magus.notes.v1alpha1.NotesService service.
type NotesServiceHandler interface {
	// ListNotes returns every note in both declared stores, each with its anchors already
	// resolved. Paginated by contract so growth never forces a breaking change, though the
	// store returns all notes today (bounded by the store's own scan cap).
	ListNotes(context.Context, *connect.Request[v1alpha1.ListNotesRequest]) (*connect.Response[v1alpha1.ListNotesResponse], error)
	// GetNote returns one note by scope and name, including its body.
	GetNote(context.Context, *connect.Request[v1alpha1.GetNoteRequest]) (*connect.Response[v1alpha1.Note], error)
}

// NewNotesServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewNotesServiceHandler(svc NotesServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	notesServiceMethods := v1alpha1.File_magus_notes_v1alpha1_notes_proto.Services().ByName("NotesService").Methods()
	notesServiceListNotesHandler := connect.NewUnaryHandler(
		NotesServiceListNotesProcedure,
		svc.ListNotes,
		connect.WithSchema(notesServiceMethods.ByName("ListNotes")),
		connect.WithHandlerOptions(opts...),
	)
	notesServiceGetNoteHandler := connect.NewUnaryHandler(
		NotesServiceGetNoteProcedure,
		svc.GetNote,
		connect.WithSchema(notesServiceMethods.ByName("GetNote")),
		connect.WithHandlerOptions(opts...),
	)
	return "/magus.notes.v1alpha1.NotesService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case NotesServiceListNotesProcedure:
			notesServiceListNotesHandler.ServeHTTP(w, r)
		case NotesServiceGetNoteProcedure:
			notesServiceGetNoteHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedNotesServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedNotesServiceHandler struct{}

func (UnimplementedNotesServiceHandler) ListNotes(context.Context, *connect.Request[v1alpha1.ListNotesRequest]) (*connect.Response[v1alpha1.ListNotesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.notes.v1alpha1.NotesService.ListNotes is not implemented"))
}

func (UnimplementedNotesServiceHandler) GetNote(context.Context, *connect.Request[v1alpha1.GetNoteRequest]) (*connect.Response[v1alpha1.Note], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.notes.v1alpha1.NotesService.GetNote is not implemented"))
}
