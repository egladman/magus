// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: magus/graph/v1alpha1/graph.proto

// Package magus.graph.v1alpha1 is the versioned wire contract for the knowledge graph the daemon
// serves to the browser Graph Explorer. Two surfaces share it: the bulk subgraph document behind
// GET /api/v1/graph (Graph/Node/Edge, mirroring types.KnowledgeGraphOutput - field names match
// that type's JSON so a protojson encoding is wire-compatible with what the page already
// consumes), and GraphService, the ranked-retrieval verbs below. A sibling of
// magus.viewer.v1alpha1 and magus.status.v1alpha1. buf-breaking gates this file.
package graphv1alpha1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1alpha1 "github.com/egladman/magus/proto/gen/go/magus/graph/v1alpha1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GraphServiceName is the fully-qualified name of the GraphService service.
	GraphServiceName = "magus.graph.v1alpha1.GraphService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GraphServiceQueryNodesProcedure is the fully-qualified name of the GraphService's QueryNodes RPC.
	GraphServiceQueryNodesProcedure = "/magus.graph.v1alpha1.GraphService/QueryNodes"
	// GraphServiceResolveNodesProcedure is the fully-qualified name of the GraphService's ResolveNodes
	// RPC.
	GraphServiceResolveNodesProcedure = "/magus.graph.v1alpha1.GraphService/ResolveNodes"
	// GraphServiceExplainNodeProcedure is the fully-qualified name of the GraphService's ExplainNode
	// RPC.
	GraphServiceExplainNodeProcedure = "/magus.graph.v1alpha1.GraphService/ExplainNode"
	// GraphServiceFindPathProcedure is the fully-qualified name of the GraphService's FindPath RPC.
	GraphServiceFindPathProcedure = "/magus.graph.v1alpha1.GraphService/FindPath"
	// GraphServiceFindDependentsProcedure is the fully-qualified name of the GraphService's
	// FindDependents RPC.
	GraphServiceFindDependentsProcedure = "/magus.graph.v1alpha1.GraphService/FindDependents"
	// GraphServiceFindAffectedProcedure is the fully-qualified name of the GraphService's FindAffected
	// RPC.
	GraphServiceFindAffectedProcedure = "/magus.graph.v1alpha1.GraphService/FindAffected"
	// GraphServiceGetGraphStatsProcedure is the fully-qualified name of the GraphService's
	// GetGraphStats RPC.
	GraphServiceGetGraphStatsProcedure = "/magus.graph.v1alpha1.GraphService/GetGraphStats"
)

// GraphServiceClient is a client for the magus.graph.v1alpha1.GraphService service.
type GraphServiceClient interface {
	// QueryNodes resolves search terms to ranked matches plus the induced neighborhood, collected
	// up to a node budget. Paginated: page with offset + len(matches) against match_count, which
	// is the TOTAL, not the page size.
	QueryNodes(context.Context, *connect.Request[v1alpha1.QueryNodesRequest]) (*connect.Response[v1alpha1.QueryNodesResponse], error)
	// ResolveNodes returns the ranked candidates for a partial reference, for completion. Cheaper
	// than QueryNodes: matches only, no neighborhood.
	ResolveNodes(context.Context, *connect.Request[v1alpha1.ResolveNodesRequest]) (*connect.Response[v1alpha1.ResolveNodesResponse], error)
	// ExplainNode returns one node's context: its data, its in/out edges with provenance, and how
	// many nodes transitively reach it. A name that resolves to nothing is NOT_FOUND.
	ExplainNode(context.Context, *connect.Request[v1alpha1.ExplainNodeRequest]) (*connect.Response[v1alpha1.NodeContext], error)
	// FindPath returns the shortest chain between two nodes, edges walked in either direction.
	// found=false is an answer, not an error; an endpoint that resolves to nothing is NOT_FOUND.
	FindPath(context.Context, *connect.Request[v1alpha1.FindPathRequest]) (*connect.Response[v1alpha1.Path], error)
	// FindDependents returns every node that transitively DEPENDS ON one, as ids - the answer to
	// "what rebuilds if I change this".
	//
	// Deliberately not NodeContext.blast_radius, which is a different question wearing a similar
	// name: that counts everything reaching a node by ANY relation. Nothing depends_on a spell (a
	// target USES one), so a spell's blast_radius runs to the hundreds while its dependents are
	// empty, and both are right. Ids rather than a count because the caller highlights them; a
	// separate RPC rather than a field on NodeContext because a hub's list is long and an explain
	// card should not carry it.
	FindDependents(context.Context, *connect.Request[v1alpha1.FindDependentsRequest]) (*connect.Response[v1alpha1.Dependents], error)
	// FindAffected returns the projects a VCS diff reaches, as graph node ids, so a viewer can
	// highlight what the working tree touches. `magus affected` over the wire.
	//
	// Read-only like the rest of the service, but the only verb here that reads the VCS rather
	// than the graph, so it is the only one whose answer changes while the graph stands still.
	FindAffected(context.Context, *connect.Request[v1alpha1.FindAffectedRequest]) (*connect.Response[v1alpha1.Affected], error)
	// GetGraphStats returns where the workspace concentrates, neglects, and fragments.
	GetGraphStats(context.Context, *connect.Request[v1alpha1.GetGraphStatsRequest]) (*connect.Response[v1alpha1.GraphStats], error)
}

// NewGraphServiceClient constructs a client for the magus.graph.v1alpha1.GraphService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGraphServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GraphServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	graphServiceMethods := v1alpha1.File_magus_graph_v1alpha1_graph_proto.Services().ByName("GraphService").Methods()
	return &graphServiceClient{
		queryNodes: connect.NewClient[v1alpha1.QueryNodesRequest, v1alpha1.QueryNodesResponse](
			httpClient,
			baseURL+GraphServiceQueryNodesProcedure,
			connect.WithSchema(graphServiceMethods.ByName("QueryNodes")),
			connect.WithClientOptions(opts...),
		),
		resolveNodes: connect.NewClient[v1alpha1.ResolveNodesRequest, v1alpha1.ResolveNodesResponse](
			httpClient,
			baseURL+GraphServiceResolveNodesProcedure,
			connect.WithSchema(graphServiceMethods.ByName("ResolveNodes")),
			connect.WithClientOptions(opts...),
		),
		explainNode: connect.NewClient[v1alpha1.ExplainNodeRequest, v1alpha1.NodeContext](
			httpClient,
			baseURL+GraphServiceExplainNodeProcedure,
			connect.WithSchema(graphServiceMethods.ByName("ExplainNode")),
			connect.WithClientOptions(opts...),
		),
		findPath: connect.NewClient[v1alpha1.FindPathRequest, v1alpha1.Path](
			httpClient,
			baseURL+GraphServiceFindPathProcedure,
			connect.WithSchema(graphServiceMethods.ByName("FindPath")),
			connect.WithClientOptions(opts...),
		),
		findDependents: connect.NewClient[v1alpha1.FindDependentsRequest, v1alpha1.Dependents](
			httpClient,
			baseURL+GraphServiceFindDependentsProcedure,
			connect.WithSchema(graphServiceMethods.ByName("FindDependents")),
			connect.WithClientOptions(opts...),
		),
		findAffected: connect.NewClient[v1alpha1.FindAffectedRequest, v1alpha1.Affected](
			httpClient,
			baseURL+GraphServiceFindAffectedProcedure,
			connect.WithSchema(graphServiceMethods.ByName("FindAffected")),
			connect.WithClientOptions(opts...),
		),
		getGraphStats: connect.NewClient[v1alpha1.GetGraphStatsRequest, v1alpha1.GraphStats](
			httpClient,
			baseURL+GraphServiceGetGraphStatsProcedure,
			connect.WithSchema(graphServiceMethods.ByName("GetGraphStats")),
			connect.WithClientOptions(opts...),
		),
	}
}

// graphServiceClient implements GraphServiceClient.
type graphServiceClient struct {
	queryNodes     *connect.Client[v1alpha1.QueryNodesRequest, v1alpha1.QueryNodesResponse]
	resolveNodes   *connect.Client[v1alpha1.ResolveNodesRequest, v1alpha1.ResolveNodesResponse]
	explainNode    *connect.Client[v1alpha1.ExplainNodeRequest, v1alpha1.NodeContext]
	findPath       *connect.Client[v1alpha1.FindPathRequest, v1alpha1.Path]
	findDependents *connect.Client[v1alpha1.FindDependentsRequest, v1alpha1.Dependents]
	findAffected   *connect.Client[v1alpha1.FindAffectedRequest, v1alpha1.Affected]
	getGraphStats  *connect.Client[v1alpha1.GetGraphStatsRequest, v1alpha1.GraphStats]
}

// QueryNodes calls magus.graph.v1alpha1.GraphService.QueryNodes.
func (c *graphServiceClient) QueryNodes(ctx context.Context, req *connect.Request[v1alpha1.QueryNodesRequest]) (*connect.Response[v1alpha1.QueryNodesResponse], error) {
	return c.queryNodes.CallUnary(ctx, req)
}

// ResolveNodes calls magus.graph.v1alpha1.GraphService.ResolveNodes.
func (c *graphServiceClient) ResolveNodes(ctx context.Context, req *connect.Request[v1alpha1.ResolveNodesRequest]) (*connect.Response[v1alpha1.ResolveNodesResponse], error) {
	return c.resolveNodes.CallUnary(ctx, req)
}

// ExplainNode calls magus.graph.v1alpha1.GraphService.ExplainNode.
func (c *graphServiceClient) ExplainNode(ctx context.Context, req *connect.Request[v1alpha1.ExplainNodeRequest]) (*connect.Response[v1alpha1.NodeContext], error) {
	return c.explainNode.CallUnary(ctx, req)
}

// FindPath calls magus.graph.v1alpha1.GraphService.FindPath.
func (c *graphServiceClient) FindPath(ctx context.Context, req *connect.Request[v1alpha1.FindPathRequest]) (*connect.Response[v1alpha1.Path], error) {
	return c.findPath.CallUnary(ctx, req)
}

// FindDependents calls magus.graph.v1alpha1.GraphService.FindDependents.
func (c *graphServiceClient) FindDependents(ctx context.Context, req *connect.Request[v1alpha1.FindDependentsRequest]) (*connect.Response[v1alpha1.Dependents], error) {
	return c.findDependents.CallUnary(ctx, req)
}

// FindAffected calls magus.graph.v1alpha1.GraphService.FindAffected.
func (c *graphServiceClient) FindAffected(ctx context.Context, req *connect.Request[v1alpha1.FindAffectedRequest]) (*connect.Response[v1alpha1.Affected], error) {
	return c.findAffected.CallUnary(ctx, req)
}

// GetGraphStats calls magus.graph.v1alpha1.GraphService.GetGraphStats.
func (c *graphServiceClient) GetGraphStats(ctx context.Context, req *connect.Request[v1alpha1.GetGraphStatsRequest]) (*connect.Response[v1alpha1.GraphStats], error) {
	return c.getGraphStats.CallUnary(ctx, req)
}

// GraphServiceHandler is an implementation of the magus.graph.v1alpha1.GraphService service.
type GraphServiceHandler interface {
	// QueryNodes resolves search terms to ranked matches plus the induced neighborhood, collected
	// up to a node budget. Paginated: page with offset + len(matches) against match_count, which
	// is the TOTAL, not the page size.
	QueryNodes(context.Context, *connect.Request[v1alpha1.QueryNodesRequest]) (*connect.Response[v1alpha1.QueryNodesResponse], error)
	// ResolveNodes returns the ranked candidates for a partial reference, for completion. Cheaper
	// than QueryNodes: matches only, no neighborhood.
	ResolveNodes(context.Context, *connect.Request[v1alpha1.ResolveNodesRequest]) (*connect.Response[v1alpha1.ResolveNodesResponse], error)
	// ExplainNode returns one node's context: its data, its in/out edges with provenance, and how
	// many nodes transitively reach it. A name that resolves to nothing is NOT_FOUND.
	ExplainNode(context.Context, *connect.Request[v1alpha1.ExplainNodeRequest]) (*connect.Response[v1alpha1.NodeContext], error)
	// FindPath returns the shortest chain between two nodes, edges walked in either direction.
	// found=false is an answer, not an error; an endpoint that resolves to nothing is NOT_FOUND.
	FindPath(context.Context, *connect.Request[v1alpha1.FindPathRequest]) (*connect.Response[v1alpha1.Path], error)
	// FindDependents returns every node that transitively DEPENDS ON one, as ids - the answer to
	// "what rebuilds if I change this".
	//
	// Deliberately not NodeContext.blast_radius, which is a different question wearing a similar
	// name: that counts everything reaching a node by ANY relation. Nothing depends_on a spell (a
	// target USES one), so a spell's blast_radius runs to the hundreds while its dependents are
	// empty, and both are right. Ids rather than a count because the caller highlights them; a
	// separate RPC rather than a field on NodeContext because a hub's list is long and an explain
	// card should not carry it.
	FindDependents(context.Context, *connect.Request[v1alpha1.FindDependentsRequest]) (*connect.Response[v1alpha1.Dependents], error)
	// FindAffected returns the projects a VCS diff reaches, as graph node ids, so a viewer can
	// highlight what the working tree touches. `magus affected` over the wire.
	//
	// Read-only like the rest of the service, but the only verb here that reads the VCS rather
	// than the graph, so it is the only one whose answer changes while the graph stands still.
	FindAffected(context.Context, *connect.Request[v1alpha1.FindAffectedRequest]) (*connect.Response[v1alpha1.Affected], error)
	// GetGraphStats returns where the workspace concentrates, neglects, and fragments.
	GetGraphStats(context.Context, *connect.Request[v1alpha1.GetGraphStatsRequest]) (*connect.Response[v1alpha1.GraphStats], error)
}

// NewGraphServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGraphServiceHandler(svc GraphServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	graphServiceMethods := v1alpha1.File_magus_graph_v1alpha1_graph_proto.Services().ByName("GraphService").Methods()
	graphServiceQueryNodesHandler := connect.NewUnaryHandler(
		GraphServiceQueryNodesProcedure,
		svc.QueryNodes,
		connect.WithSchema(graphServiceMethods.ByName("QueryNodes")),
		connect.WithHandlerOptions(opts...),
	)
	graphServiceResolveNodesHandler := connect.NewUnaryHandler(
		GraphServiceResolveNodesProcedure,
		svc.ResolveNodes,
		connect.WithSchema(graphServiceMethods.ByName("ResolveNodes")),
		connect.WithHandlerOptions(opts...),
	)
	graphServiceExplainNodeHandler := connect.NewUnaryHandler(
		GraphServiceExplainNodeProcedure,
		svc.ExplainNode,
		connect.WithSchema(graphServiceMethods.ByName("ExplainNode")),
		connect.WithHandlerOptions(opts...),
	)
	graphServiceFindPathHandler := connect.NewUnaryHandler(
		GraphServiceFindPathProcedure,
		svc.FindPath,
		connect.WithSchema(graphServiceMethods.ByName("FindPath")),
		connect.WithHandlerOptions(opts...),
	)
	graphServiceFindDependentsHandler := connect.NewUnaryHandler(
		GraphServiceFindDependentsProcedure,
		svc.FindDependents,
		connect.WithSchema(graphServiceMethods.ByName("FindDependents")),
		connect.WithHandlerOptions(opts...),
	)
	graphServiceFindAffectedHandler := connect.NewUnaryHandler(
		GraphServiceFindAffectedProcedure,
		svc.FindAffected,
		connect.WithSchema(graphServiceMethods.ByName("FindAffected")),
		connect.WithHandlerOptions(opts...),
	)
	graphServiceGetGraphStatsHandler := connect.NewUnaryHandler(
		GraphServiceGetGraphStatsProcedure,
		svc.GetGraphStats,
		connect.WithSchema(graphServiceMethods.ByName("GetGraphStats")),
		connect.WithHandlerOptions(opts...),
	)
	return "/magus.graph.v1alpha1.GraphService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GraphServiceQueryNodesProcedure:
			graphServiceQueryNodesHandler.ServeHTTP(w, r)
		case GraphServiceResolveNodesProcedure:
			graphServiceResolveNodesHandler.ServeHTTP(w, r)
		case GraphServiceExplainNodeProcedure:
			graphServiceExplainNodeHandler.ServeHTTP(w, r)
		case GraphServiceFindPathProcedure:
			graphServiceFindPathHandler.ServeHTTP(w, r)
		case GraphServiceFindDependentsProcedure:
			graphServiceFindDependentsHandler.ServeHTTP(w, r)
		case GraphServiceFindAffectedProcedure:
			graphServiceFindAffectedHandler.ServeHTTP(w, r)
		case GraphServiceGetGraphStatsProcedure:
			graphServiceGetGraphStatsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGraphServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGraphServiceHandler struct{}

func (UnimplementedGraphServiceHandler) QueryNodes(context.Context, *connect.Request[v1alpha1.QueryNodesRequest]) (*connect.Response[v1alpha1.QueryNodesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.QueryNodes is not implemented"))
}

func (UnimplementedGraphServiceHandler) ResolveNodes(context.Context, *connect.Request[v1alpha1.ResolveNodesRequest]) (*connect.Response[v1alpha1.ResolveNodesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.ResolveNodes is not implemented"))
}

func (UnimplementedGraphServiceHandler) ExplainNode(context.Context, *connect.Request[v1alpha1.ExplainNodeRequest]) (*connect.Response[v1alpha1.NodeContext], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.ExplainNode is not implemented"))
}

func (UnimplementedGraphServiceHandler) FindPath(context.Context, *connect.Request[v1alpha1.FindPathRequest]) (*connect.Response[v1alpha1.Path], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.FindPath is not implemented"))
}

func (UnimplementedGraphServiceHandler) FindDependents(context.Context, *connect.Request[v1alpha1.FindDependentsRequest]) (*connect.Response[v1alpha1.Dependents], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.FindDependents is not implemented"))
}

func (UnimplementedGraphServiceHandler) FindAffected(context.Context, *connect.Request[v1alpha1.FindAffectedRequest]) (*connect.Response[v1alpha1.Affected], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.FindAffected is not implemented"))
}

func (UnimplementedGraphServiceHandler) GetGraphStats(context.Context, *connect.Request[v1alpha1.GetGraphStatsRequest]) (*connect.Response[v1alpha1.GraphStats], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.graph.v1alpha1.GraphService.GetGraphStats is not implemented"))
}
