// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: magus/insight/v1/insight.proto

// Package magus.insight.v1 is the versioned wire contract for magus insight: where a
// codebase's attention and risk concentrate. Five lenses ride one message - four read VCS
// history (hotspots, affinity, ownership, trend) from a single bounded git-log scan the
// daemon caches, and volatility reads run-outcome history instead. They travel together
// because the dashboard renders them as one board; splitting them into five RPCs would cost
// five round trips for one paint and would let the four git lenses disagree about the window
// they were scanned over.
//
// It is a sibling of the other console tool-page contracts (magus.status.v1, magus.activity.v1,
// magus.metrics.v1). It mirrors types.InsightView, which the CLI also renders; the mapping to
// this wire lives in the handler, so the domain type stays free of protobuf. buf-breaking
// gates this file.
//
// It deliberately does NOT carry the knowledge-graph axis (types.InsightReport's graph_stats):
// the console read never touches the knowledge graph, and folding it in here would make every
// dashboard poll pay for a graph load.
package insightv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/egladman/magus/proto/gen/go/magus/insight/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// InsightServiceName is the fully-qualified name of the InsightService service.
	InsightServiceName = "magus.insight.v1.InsightService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// InsightServiceGetInsightProcedure is the fully-qualified name of the InsightService's GetInsight
	// RPC.
	InsightServiceGetInsightProcedure = "/magus.insight.v1.InsightService/GetInsight"
)

// InsightServiceClient is a client for the magus.insight.v1.InsightService service.
type InsightServiceClient interface {
	// GetInsight returns every lens in one message.
	GetInsight(context.Context, *connect.Request[v1.GetInsightRequest]) (*connect.Response[v1.GetInsightResponse], error)
}

// NewInsightServiceClient constructs a client for the magus.insight.v1.InsightService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewInsightServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) InsightServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	insightServiceMethods := v1.File_magus_insight_v1_insight_proto.Services().ByName("InsightService").Methods()
	return &insightServiceClient{
		getInsight: connect.NewClient[v1.GetInsightRequest, v1.GetInsightResponse](
			httpClient,
			baseURL+InsightServiceGetInsightProcedure,
			connect.WithSchema(insightServiceMethods.ByName("GetInsight")),
			connect.WithClientOptions(opts...),
		),
	}
}

// insightServiceClient implements InsightServiceClient.
type insightServiceClient struct {
	getInsight *connect.Client[v1.GetInsightRequest, v1.GetInsightResponse]
}

// GetInsight calls magus.insight.v1.InsightService.GetInsight.
func (c *insightServiceClient) GetInsight(ctx context.Context, req *connect.Request[v1.GetInsightRequest]) (*connect.Response[v1.GetInsightResponse], error) {
	return c.getInsight.CallUnary(ctx, req)
}

// InsightServiceHandler is an implementation of the magus.insight.v1.InsightService service.
type InsightServiceHandler interface {
	// GetInsight returns every lens in one message.
	GetInsight(context.Context, *connect.Request[v1.GetInsightRequest]) (*connect.Response[v1.GetInsightResponse], error)
}

// NewInsightServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewInsightServiceHandler(svc InsightServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	insightServiceMethods := v1.File_magus_insight_v1_insight_proto.Services().ByName("InsightService").Methods()
	insightServiceGetInsightHandler := connect.NewUnaryHandler(
		InsightServiceGetInsightProcedure,
		svc.GetInsight,
		connect.WithSchema(insightServiceMethods.ByName("GetInsight")),
		connect.WithHandlerOptions(opts...),
	)
	return "/magus.insight.v1.InsightService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case InsightServiceGetInsightProcedure:
			insightServiceGetInsightHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedInsightServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedInsightServiceHandler struct{}

func (UnimplementedInsightServiceHandler) GetInsight(context.Context, *connect.Request[v1.GetInsightRequest]) (*connect.Response[v1.GetInsightResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.insight.v1.InsightService.GetInsight is not implemented"))
}
