// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: magus/status/v1/status.proto

// Package magus.status.v1 is the versioned wire contract for magus's status/dashboard
// view, scoped to LIVE state: overall health, the concurrency pool (capacity/running/
// queued slots), what is running right now - the running targets, their workspace, and
// how long they have run - and live cache ACTIVITY (hit/miss/error tallies + real on-disk
// size). Static CONFIG (telemetry, the cache cap/immutability, build flags) is deliberately
// NOT here; that is `magus status`/config, not "what is happening." Backs a native-feeling
// /dashboard. A sibling of magus.viewer.v1. buf-breaking gates this file.
package statusv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/egladman/magus/proto/gen/go/magus/status/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StatusServiceName is the fully-qualified name of the StatusService service.
	StatusServiceName = "magus.status.v1.StatusService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StatusServiceGetStatusProcedure is the fully-qualified name of the StatusService's GetStatus RPC.
	StatusServiceGetStatusProcedure = "/magus.status.v1.StatusService/GetStatus"
	// StatusServiceStreamStatusProcedure is the fully-qualified name of the StatusService's
	// StreamStatus RPC.
	StatusServiceStreamStatusProcedure = "/magus.status.v1.StatusService/StreamStatus"
)

// StatusServiceClient is a client for the magus.status.v1.StatusService service.
type StatusServiceClient interface {
	// GetStatus returns the current snapshot.
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
	// StreamStatus pushes a fresh snapshot whenever the pool changes (or on a heartbeat),
	// so a dashboard reflects what is running without polling.
	StreamStatus(context.Context, *connect.Request[v1.StreamStatusRequest]) (*connect.ServerStreamForClient[v1.StreamStatusResponse], error)
}

// NewStatusServiceClient constructs a client for the magus.status.v1.StatusService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStatusServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StatusServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	statusServiceMethods := v1.File_magus_status_v1_status_proto.Services().ByName("StatusService").Methods()
	return &statusServiceClient{
		getStatus: connect.NewClient[v1.GetStatusRequest, v1.GetStatusResponse](
			httpClient,
			baseURL+StatusServiceGetStatusProcedure,
			connect.WithSchema(statusServiceMethods.ByName("GetStatus")),
			connect.WithClientOptions(opts...),
		),
		streamStatus: connect.NewClient[v1.StreamStatusRequest, v1.StreamStatusResponse](
			httpClient,
			baseURL+StatusServiceStreamStatusProcedure,
			connect.WithSchema(statusServiceMethods.ByName("StreamStatus")),
			connect.WithClientOptions(opts...),
		),
	}
}

// statusServiceClient implements StatusServiceClient.
type statusServiceClient struct {
	getStatus    *connect.Client[v1.GetStatusRequest, v1.GetStatusResponse]
	streamStatus *connect.Client[v1.StreamStatusRequest, v1.StreamStatusResponse]
}

// GetStatus calls magus.status.v1.StatusService.GetStatus.
func (c *statusServiceClient) GetStatus(ctx context.Context, req *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return c.getStatus.CallUnary(ctx, req)
}

// StreamStatus calls magus.status.v1.StatusService.StreamStatus.
func (c *statusServiceClient) StreamStatus(ctx context.Context, req *connect.Request[v1.StreamStatusRequest]) (*connect.ServerStreamForClient[v1.StreamStatusResponse], error) {
	return c.streamStatus.CallServerStream(ctx, req)
}

// StatusServiceHandler is an implementation of the magus.status.v1.StatusService service.
type StatusServiceHandler interface {
	// GetStatus returns the current snapshot.
	GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error)
	// StreamStatus pushes a fresh snapshot whenever the pool changes (or on a heartbeat),
	// so a dashboard reflects what is running without polling.
	StreamStatus(context.Context, *connect.Request[v1.StreamStatusRequest], *connect.ServerStream[v1.StreamStatusResponse]) error
}

// NewStatusServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStatusServiceHandler(svc StatusServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	statusServiceMethods := v1.File_magus_status_v1_status_proto.Services().ByName("StatusService").Methods()
	statusServiceGetStatusHandler := connect.NewUnaryHandler(
		StatusServiceGetStatusProcedure,
		svc.GetStatus,
		connect.WithSchema(statusServiceMethods.ByName("GetStatus")),
		connect.WithHandlerOptions(opts...),
	)
	statusServiceStreamStatusHandler := connect.NewServerStreamHandler(
		StatusServiceStreamStatusProcedure,
		svc.StreamStatus,
		connect.WithSchema(statusServiceMethods.ByName("StreamStatus")),
		connect.WithHandlerOptions(opts...),
	)
	return "/magus.status.v1.StatusService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StatusServiceGetStatusProcedure:
			statusServiceGetStatusHandler.ServeHTTP(w, r)
		case StatusServiceStreamStatusProcedure:
			statusServiceStreamStatusHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStatusServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedStatusServiceHandler struct{}

func (UnimplementedStatusServiceHandler) GetStatus(context.Context, *connect.Request[v1.GetStatusRequest]) (*connect.Response[v1.GetStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.status.v1.StatusService.GetStatus is not implemented"))
}

func (UnimplementedStatusServiceHandler) StreamStatus(context.Context, *connect.Request[v1.StreamStatusRequest], *connect.ServerStream[v1.StreamStatusResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("magus.status.v1.StatusService.StreamStatus is not implemented"))
}
