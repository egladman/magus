// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: magus/tool/v1alpha1/tool.proto

// Package magus.tool.v1alpha1 is the versioned wire contract for the toolchain view: which
// binaries a workspace's spells drive, what version each one actually reported, and the
// version window it is held to. It answers "is the toolchain this build ran on the one
// this repo says it supports", which the CLI answers one op at a time (MGS3005/MGS3006)
// and which nothing could answer for the workspace as a whole.
//
// Everything here is a READ of state magus already builds: the probe is a cache-key input
// that runs on every build regardless, and the window comes from a spell's `supported`
// plus a project's `tools` key. magus does not learn which versions exist upstream, does
// not select one, and carries no end-of-life data - see docs/scope.md. A future eol field
// would be an ADDITION to Tool, which proto3 permits without a wire break, so its absence
// here costs nothing later.
//
// A sibling of magus.status.v1alpha1: status is "what is happening right now", this is "what is
// this workspace built with". buf-breaking gates this file.
package toolv1alpha1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1alpha1 "github.com/egladman/magus/proto/gen/go/magus/tool/v1alpha1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ToolServiceName is the fully-qualified name of the ToolService service.
	ToolServiceName = "magus.tool.v1alpha1.ToolService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ToolServiceListToolsProcedure is the fully-qualified name of the ToolService's ListTools RPC.
	ToolServiceListToolsProcedure = "/magus.tool.v1alpha1.ToolService/ListTools"
)

// ToolServiceClient is a client for the magus.tool.v1alpha1.ToolService service.
type ToolServiceClient interface {
	// ListTools returns every project's tools with their windows and verdicts.
	//
	// The name does not match the repeated field (projects), which AIP-132 would have it do.
	// Left deliberately: this returns a two-level view, projects each carrying their tools,
	// because a tool version is a per-project fact and the dashboard renders it grouped that
	// way. Flattening to `repeated Tool` to satisfy the rule would make every Tool carry its
	// own project path and lose the grouping; renaming this ListProjects would leave the
	// toolchain service with no RPC that mentions a tool. AIP-132 governs a collection of one
	// resource, and this is not one.
	ListTools(context.Context, *connect.Request[v1alpha1.ListToolsRequest]) (*connect.Response[v1alpha1.ListToolsResponse], error)
}

// NewToolServiceClient constructs a client for the magus.tool.v1alpha1.ToolService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewToolServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ToolServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	toolServiceMethods := v1alpha1.File_magus_tool_v1alpha1_tool_proto.Services().ByName("ToolService").Methods()
	return &toolServiceClient{
		listTools: connect.NewClient[v1alpha1.ListToolsRequest, v1alpha1.ListToolsResponse](
			httpClient,
			baseURL+ToolServiceListToolsProcedure,
			connect.WithSchema(toolServiceMethods.ByName("ListTools")),
			connect.WithClientOptions(opts...),
		),
	}
}

// toolServiceClient implements ToolServiceClient.
type toolServiceClient struct {
	listTools *connect.Client[v1alpha1.ListToolsRequest, v1alpha1.ListToolsResponse]
}

// ListTools calls magus.tool.v1alpha1.ToolService.ListTools.
func (c *toolServiceClient) ListTools(ctx context.Context, req *connect.Request[v1alpha1.ListToolsRequest]) (*connect.Response[v1alpha1.ListToolsResponse], error) {
	return c.listTools.CallUnary(ctx, req)
}

// ToolServiceHandler is an implementation of the magus.tool.v1alpha1.ToolService service.
type ToolServiceHandler interface {
	// ListTools returns every project's tools with their windows and verdicts.
	//
	// The name does not match the repeated field (projects), which AIP-132 would have it do.
	// Left deliberately: this returns a two-level view, projects each carrying their tools,
	// because a tool version is a per-project fact and the dashboard renders it grouped that
	// way. Flattening to `repeated Tool` to satisfy the rule would make every Tool carry its
	// own project path and lose the grouping; renaming this ListProjects would leave the
	// toolchain service with no RPC that mentions a tool. AIP-132 governs a collection of one
	// resource, and this is not one.
	ListTools(context.Context, *connect.Request[v1alpha1.ListToolsRequest]) (*connect.Response[v1alpha1.ListToolsResponse], error)
}

// NewToolServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewToolServiceHandler(svc ToolServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	toolServiceMethods := v1alpha1.File_magus_tool_v1alpha1_tool_proto.Services().ByName("ToolService").Methods()
	toolServiceListToolsHandler := connect.NewUnaryHandler(
		ToolServiceListToolsProcedure,
		svc.ListTools,
		connect.WithSchema(toolServiceMethods.ByName("ListTools")),
		connect.WithHandlerOptions(opts...),
	)
	return "/magus.tool.v1alpha1.ToolService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ToolServiceListToolsProcedure:
			toolServiceListToolsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedToolServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedToolServiceHandler struct{}

func (UnimplementedToolServiceHandler) ListTools(context.Context, *connect.Request[v1alpha1.ListToolsRequest]) (*connect.Response[v1alpha1.ListToolsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("magus.tool.v1alpha1.ToolService.ListTools is not implemented"))
}
