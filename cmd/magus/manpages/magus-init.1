.TH MAGUS-INIT 1 "" "magus dev" "magus Manual"
.SH NAME
magus\-init \- Bootstrap a workspace (magus.yaml + magusfile.tl + merge driver)
.SH SYNOPSIS
.B 
.SH DESCRIPTION
Bootstrap a magus workspace in the current directory.
.PP
By default, magus.yaml is written to $XDG_CONFIG_HOME/magus/ (the global user
config location). Use \-\-local to write it into the repo instead (useful for
checked\-in, team\-shared config). The magusfile stub and VCS merge driver are
always wired in the repo.
.PP
With \-\-global only the global config is written; the per\-clone workspace
bootstrap (magusfile stub + merge driver) is skipped.
.PP
The VCS is taken from \-\-vcs, or chosen interactively when stdin is a terminal.
.SH OPTIONS
.RS
.TP
\fB\-\-force\fR
Overwrite an existing config file
.TP
\fB\-\-global\fR
Write only the global config; skip the workspace bootstrap
.TP
\fB\-\-local\fR
Write config into the repo (CWD) instead of $XDG_CONFIG_HOME/magus/
.TP
\fB\-\-vcs\fR \fIstring\fR
VCS to wire the merge driver for (git|hg); prompts when omitted on a TTY
.RE
.SH EXAMPLES
\fIBootstrap the current repo\fR
.PP
.EX
magus init
.EE
.PP
\fINon\-interactive (CI): pick the VCS explicitly\fR
.PP
.EX
magus init --vcs git
.EE
.PP
\fIWrite only the global config\fR
.PP
.EX
magus init --global
.EE
.PP
\fIWrite config into the repo instead of XDG\fR
.PP
.EX
magus init --local
.EE
.PP
.SH SEE ALSO
\fBmagus\fR(1),
\fBmagus\-ls\fR(1),
\fBmagus\-describe\fR(1),
\fBmagus\-run\fR(1),
\fBmagus\-x\fR(1),
\fBmagus\-where\fR(1),
\fBmagus\-tail\fR(1),
\fBmagus\-affected\fR(1),
\fBmagus\-insight\fR(1),
\fBmagus\-watch\fR(1),
\fBmagus\-status\fR(1),
\fBmagus\-doctor\fR(1),
\fBmagus\-config\fR(1),
\fBmagus\-server\fR(1),
\fBmagus\-completion\fR(1),
\fBmagus\-self\fR(1),
\fBmagus\-version\fR(1)
.br
