// A magusfile defines targets: exported functions magus can run. This is the
// smallest one. Try it in the console below - type `ls`, then `run greet`.

import "magus";

export fun greet(args: [str]) > void {
    magus.info("hello from magus");
}
