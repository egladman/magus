name: CD

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read  # default; the release job overrides to write

jobs:
  release:
    name: Build and publish release
    runs-on: ubuntu-latest
    permissions:
      contents: write  # create the GitHub Release and upload assets
    env:
      # Build CGO-free across the whole job (matches the shipped static binary and
      # uses internal/codec's pure-Go xz/zstd fallbacks), so the runner needs no
      # liblzma/libzstd. Covers setup-magus's build, the release cross-compiles, and
      # the magus-utils signer - all of which otherwise pull in the cgo codec.
      CGO_ENABLED: "0"
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          # Full history + tags so the release target's version() (git describe
          # --tags) resolves the tag and build_date() reads the commit date.
          fetch-depth: 0

      - uses: ./.github/actions/setup-magus

      # No remote-cache env is set in CD on purpose: a release builds clean off the
      # cache. magusfile.buzz leaves the backend unbound without it, so there
      # is zero chance of replaying a cached (unsigned) artifact into a release.
      # Cross-compiles all 5 targets into dist/ and writes SHA256SUMS.
      - name: Build release artifacts
        run: magus run release --no-default-charms

      - name: Sign SHA256SUMS
        env:
          MAGUS_SIGNING_KEY: ${{ secrets.MAGUS_SIGNING_KEY }}
        run: go run ./cmd/magus-utils sign dist/SHA256SUMS

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          files: |
            dist/magus_*.tar.gz
            dist/SHA256SUMS
            dist/SHA256SUMS.sig
          generate_release_notes: true

  image:
    name: Publish container images
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write  # push to GHCR
      id-token: write  # cosign keyless (OIDC) signing

    # The whole build → push → sign flow lives in the magusfile's image-build
    # target (docker + cosign spells); `magus run image-build:cd` builds the
    # static (multi-arch) and CGO (amd64) variants, pushes them, and cosign-signs
    # each pushed digest. The same command signs images locally. cosign comes from
    # mise (.mise.toml) — no cosign-specific action.
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          # Full history + tags so version()/commit()/build_date() resolve.
          fetch-depth: 0

      - uses: ./.github/actions/setup-magus

      - uses: jdx/mise-action@c37c93293d6b742fc901e1406b8f764f6fb19dac # v2.4.4

      - uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3.7.0
      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build, push, and sign images
        run: magus run image-build:cd --no-default-charms
