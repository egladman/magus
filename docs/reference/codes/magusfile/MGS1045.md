---
title: "MGS1045: a magus\\guard.spawn or magus\\guard.command registration is unusable"
description: Fires when magus\guard.spawn or magus\guard.command is handed something other than a function, is called twice in one load, or is called from a magusfile other than the root one. The workspace does not load.
tags: [MGS1045, magusfile, guard, agents, spawn, command]
---

# MGS1045: a magus\guard.spawn or magus\guard.command registration is unusable

The root magusfile registers a spawn or command rule in a way magus cannot use:

```text
[error] [MGS1045] magus\guard.spawn: already registered in this magusfile; a workspace
has one such rule, so fold the second one into the first
```

## Why

`magus\guard.spawn` and `magus\guard.command` each hand the agent guard one function to
call on every spawn, or every shell command. A registration that is ignored is worse
than none: the workspace reads as guarded and nothing is judged. So each of these stops
the load instead:

- the argument is not a function;
- a second call to the same member in the same load, which would leave it unclear which
  rule runs;
- a call from a project's magusfile rather than the workspace root's, since the rule
  answers for every agent in the workspace.

## What to do

Register exactly one function per member, from the root `magusfile.buzz`. To combine
several checks, call them from that one function and return the strictest answer. See
[magus\guard.spawn](../../guard-spawn.md) and [magus\guard.command](../../guard-command.md).
