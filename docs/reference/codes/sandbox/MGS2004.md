---
title: "MGS2004: sandbox config unresolved"
description: Fires when a sandbox.allow entry or sandbox.env.passthrough pattern in magus.yaml cannot be resolved or parsed; the run stops before anything executes.
tags: [MGS2004, sandbox, configuration, allowlist, path resolution, magus.yaml]
---

# MGS2004: sandbox config failed to resolve

A `sandbox.allow` entry or a `sandbox.env.passthrough` pattern in
`magus.yaml` could not be turned into a rule. magus stops before running
anything.

```text
[MGS2004] sandbox config for /home/user/project: sandbox: "$NONEXISTENT_VAR/cache": variable is unset or empty: NONEXISTENT_VAR
```

## Why

`sandbox.allow` paths support `~` for the home directory and `$VAR`
references resolved against the current environment. An unset or empty
variable expands to nothing, and `$UNSET/` would then grant `/`. A mode
outside `ro`, `rw`, `rx` and `rwx` (a typo such as `RW`) would grant
something other than what was written. A passthrough pattern such as
`GO*` matches more than it names (`GOOGLE_APPLICATION_CREDENTIALS`).
Each of these is refused rather than skipped: a sandbox that silently
grants less breaks builds nobody can trace, and one that grants more is
not a sandbox.

Every bad entry is reported at once.

## Resolution

- Check that any `$VAR` referenced in the path is set, and not empty, in
  the environment magus runs in, or replace it with an absolute path.
- Use `ro`, `rw`, `rx` or `rwx` for `mode`. Exec is never implied, so a
  directory of tools needs `rx`.
- Write a passthrough prefix as at least three characters ending in `_`
  (`MISE_*`, `LC_*`), or list the variables by name.
- Remove the entry if it was a typo.

## Also fires

When the job store cannot be read while narrowing the sandbox to a
lease, magus logs this code as a warning and keeps the workspace's write
grant, matching the guard: a lease that stops resolving must not brick
the checkout a person is working in.
