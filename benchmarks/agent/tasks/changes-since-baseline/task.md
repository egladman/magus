# Catch me up on what landed since release-2

The tag `release-2` marks where I stopped paying attention. Work out what has
changed since then. Change no code.

Write the answer to `ANSWER.md` at the root of this checkout, in exactly this shape:

## Answer

- packages/some-group/some-package
- apps/some-app

## Behavior change

One sentence naming the setting whose default changed, and its new value.

Under `## Answer`, list every directory with its own `package.json` whose OWN files
changed since the tag. Not the directories that merely depend on one of them. One
path per bullet and no other bullets; the list is graded as a set.
