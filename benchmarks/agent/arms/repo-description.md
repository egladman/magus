# CLAUDE.md

A TypeScript monorepo. Applications live under `apps/`, the libraries they
import under `packages/`. Several applications share the same libraries, so a
library change reaches more than one application.
