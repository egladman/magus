// Next.js app spell for the large-monorepo benchmark.
// Non-opaque, so app sources are hashed into the cache key; .next/** is declared
// as output (excluded from inputs, snapshotted for replay). setup.sh copies it to
// the checkout's spells/, and each per-app magusfile.buzz picks it up with
// `import "spells/nextjs" as nextjs`.
import "magus/spell";

export fun mgs_getName() > str { return "nextjs"; }

export fun mgs_listRequiredGlobs() > [Path] {
    return [Path{value = "**/*.ts"}, Path{value = "**/*.tsx"}, Path{value = "**/*.js"}, Path{value = "**/*.jsx"}, Path{value = "**/*.json"},
            Path{value = "package.json"},
            Path{value = "next.config.js"}, Path{value = "next.config.mjs"}, Path{value = "next.config.ts"},
            Path{value = "package-lock.json"}];
}

export fun mgs_listProvidedGlobs() > [Path] { return [Path{value = ".next/**"}]; }

export fun mgs_getVersionProbe() > [str] { return ["node", "--version"]; }

fun nextBuild(target: Target) > Command { return Command{bin = "npm", args = ["run", "build"]}; }

export fun mgs_listTargets() > {str: fun(Target) Command} {
    return {"next-build": nextBuild};
}
