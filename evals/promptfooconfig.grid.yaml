description: Full agent-skill permutation grid
prompts:
  - "{{prompt}}"
providers:
  - id: anthropic:claude-agent-sdk
    label: full/claude-sonnet-5
    config:
      model: claude-sonnet-5
      working_dir: ./fixtures/full
      setting_sources: [project]
      skills: all
      append_allowed_tools: [Bash]
      permission_mode: acceptEdits
      persist_session: false
  - id: anthropic:claude-agent-sdk
    label: simple/claude-sonnet-5
    config:
      model: claude-sonnet-5
      working_dir: ./fixtures/simple
      setting_sources: [project]
      skills: all
      append_allowed_tools: [Bash]
      permission_mode: acceptEdits
      persist_session: false
  - id: anthropic:claude-agent-sdk
    label: full/claude-haiku-4-5-20251001
    config:
      model: claude-haiku-4-5-20251001
      working_dir: ./fixtures/full
      setting_sources: [project]
      skills: all
      append_allowed_tools: [Bash]
      permission_mode: acceptEdits
      persist_session: false
  - id: anthropic:claude-agent-sdk
    label: simple/claude-haiku-4-5-20251001
    config:
      model: claude-haiku-4-5-20251001
      working_dir: ./fixtures/simple
      setting_sources: [project]
      skills: all
      append_allowed_tools: [Bash]
      permission_mode: acceptEdits
      persist_session: false
tests:
  - file://lib/tasks.mjs:loadTasks
